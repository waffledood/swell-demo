# The LangGraph deployment this app's /api gateway forwards requests to.
# Kept server-side only (no NEXT_PUBLIC_ prefix) so it's never sent to the browser.
LANGGRAPH_API_URL=

# LangSmith/LangGraph Platform API key, attached to forwarded requests server-side.
LANGSMITH_API_KEY=
